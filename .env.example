# Copy to .env and fill in values
KOGB_STORAGE_CONNECTION_STRING=
GET_PRICES_TTL_SECONDS=30
GET_HISTORY_TTL_SECONDS=180
DISABLE_HISTORY_CACHE=0
# For Azure Functions deployment, set these in application settings instead of .env
